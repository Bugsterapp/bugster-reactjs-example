name: Contact Form Validation
page: /contact
page_path: app/routes/contact.tsx
task: Test form validation on the contact form
steps:
- Navigate to the Contact page
- Leave required fields empty
- Click the submit button
- Verify validation error messages are displayed for empty fields
expected_result: Form shows appropriate validation errors for empty required fields
